### JDBC 批量入仓：导入校验接口 (Validate) 测试文件构造协议

- **1. 前置依赖与数据链路 (Prerequisites)**
在构造导入文件之前，AI 必须完成以下链式操作：
    1.  **物理表生成**: 调用 `test-table` 技能在指定实例中创建物理表。
    2.  **元数据完善**: 调用 `metadata-complete` 技能完成该表的元数据注册、字段属性补全及血缘挂载。
    3.  **参数采集**: 实时获取 `test-table` 返回的：**实例名、数据库名、表名**。

- **2. 文件存储规范 (Storage Path)**
生成的 `.xlsx` 文件必须统一存放于以下路径，以便后续 `validate` 接口读取：
> **存储位置**: `JBDC入仓/BIZ_REQ_33706_001_批量入仓_新增任务/test_data`

- **3. 接口关联性**
    - **目标接口**: `/dataops/etlx/batch/v2/validate`
    - **业务场景**: 批量任务初始化上传阶段，系统对文件内的 23 个字段执行格式、联动性及物理连通性校验。

- **4. 导入文件字段清单与取值逻辑**

| 序号 | 字段名称 (Header) | 取值逻辑 (Value Logic) | 默认值 / 示例 |
| :--- | :--- | :--- | :--- |
| 1 | 数据源类型 | 支持 `mysql`, `adb`, `tidb`。默认为 `mysql`。 | `mysql` |
| 2 | 实例 | **联动项**：取自 `test-table` 生成的实例名（强制小写）。 | `cjjcommon` |
| 3 | 库 | **联动项**：取自 `test-table` 生成的数据库名（强制小写）。 | `dataops-shitinhgjie` |
| 4 | 表 | **联动项**：取自 `test-table` 生成的表名（强制小写）。 | `testtable001` |
| 5-10 | 人员信息 | 业务/技术/创建人及其 UID（固定值）。 | `施婷杰` / `71e8b2...` |
| 11 | 用户旅程节点 | 曝光、点击、注册、登录、进件、授信、借款、还款等（**随机选一**）。 | `风险审核` |
| 12 | 需求目的 | 固定值 | `自动化测试` |
| 13 | 抽数方式 | **联动项**：全量为 `all`；增量为 `ins`。 | `ins` |
| 14 | 处理方式 | **联动项**：见下方 [抽数/处理联动逻辑]。 | `merge` |
| 15 | 调度周期 | 固定值 | `day` |
| 16 | 调度时间 | 格式为 `HH:mm`（随机生成时间点）。 | `04:20` |
| 17 | CPU | 固定值 | `1` |
| 18 | 内存 | 固定值 | `2048` |
| 19 | 创建时间字段 | 默认 created_at | `created_at` |
| 20 | 更新时间字段 | 默认 updated_at | `updated_at` |
| 21 | 批量条数 | 默认 1024 | `1024` |
| 22 | 抽数数据源 | 拼接格式：`input_{数据源类型}_{实例}_{库}` | `input_mysql_cjjcommon_...` |
| 23 | 抽数主键 | 默认 id | `id` |

- **5. 抽数/处理联动逻辑 (Dependency Rules)**
构造第 13、14 字段时，必须严格遵守以下组合模式：
    *   **全量覆盖模式**: 抽数方式 = `all` 且 处理方式 = `all`。
    *   **增量合并模式**: 抽数方式 = `ins` 且 处理方式 = `merge`。
    *   **增量分区模式**: 抽数方式 = `ins` 且 处理方式 = `ins`。

- **6. 元数据验证 SQL**
构造文件前，Skill 需执行以下查询以验证 `metadata-complete` 技能的执行效果：

```sql
SELECT instance_name, db_name, table_name, status 
FROM dataops_extract_input_datasource_config_info 
WHERE instance_name = '{instance}' AND table_name = '{table}';
```

#### 数据库连接信息:
- **Host**: `bigdata-biz.db.ali-bj-bdsit01.shuheo.net` / **Database**: `dataops`
- **User/Password**: `bdsit_user_0e0bc33` / `bdsit_user_0e0bc33_26587a`

- **7. 验证 Check 点 (Checklist)**
    *   [ ] **技能闭环**: 是否已顺序执行 `test-table` 与 `metadata-complete`？
    *   [ ] **路径核对**: 文件是否已保存至 `JBDC入仓/.../test_data` 目录下？
    *   [ ] **数据源拼接**: 第 22 列 `抽数数据源` 是否已根据前三列内容动态生成？
    *   [ ] **大小写转换**: 实例、库、表字段是否已全部强制转换为小写？
    *   [ ] **列数与格式**: 检查文件后缀是否为 `.xlsx` 且总列数是否准确为 23 列。

---
**提示**: AI 技能在完成文件构造后，应主动向用户反馈文件存储的完整路径，以便用户在执行 `/validate` 接口测试时进行引用。